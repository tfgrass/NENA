# for local testing
services:
  attachment-converter:
    build: .
    volumes:
      - ./testfolder:/app/uploads
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DEBUG=true
    tty: true
